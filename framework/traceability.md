# Traceability

The minimum traceability spine is:

`REQ → DEC → TASK → CODE → TEST → EVIDENCE`

Each link should be stable, reviewable, and bidirectional where practical. Missing links are not automatically defects: record the reason and risk. Closed decisions are immutable by default; reopening requires a new evidence item, change trigger, impact assessment, and explicit decision update.

